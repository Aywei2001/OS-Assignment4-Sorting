#include <studio.h>
#include <stdlib.h>
#include <pthread.h>

typedef struct{
	int list_start;
	int list_end;
	int * list;
}Sortparameters;

void* sortarrays(void * parameter){
	Sortparameters *data = (Sortparameters *) malloc(sizeof(parameters));
}

void* sort_2(void * parameter){
	Sortparameters *data = (Sortparameters *) malloc(sizeof(parameters));
}

int main(){
	printf("Hellow World\n");
	return 0;
}
